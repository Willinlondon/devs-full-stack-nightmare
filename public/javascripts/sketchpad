game.battle.player2.name

function enemyDisplayBattle() {
  if (game.battle.player2.name = 'Buggar')
  { idleMinotaur2.show();
    enemyImg.hide(); }
  else {
    enemyImg.show();
    idleMinotaur2.hide();
  }
}